# Exercise 5: Set up CI/CD pipeline with GitHub Actions for the web app

### Estimated Duration: 80 Minutes

In this exercise, you will move the codebase to a GitHub repository, create a staging environment in Azure App Service using deployment slots, and connect the two with a CI/CD pipeline built on GitHub Actions.

### Objectives

In this exercise, you will complete the following tasks:
   - Task 1: Moving the codebase to a GitHub repo
   - Task 2: Creating a staging deployment slot
   - Task 3: Setting up CI/CD with GitHub Actions
   - Task 4: Pushing code changes to staging and production
   - Task 5: Swap deployment slots to move changes in staging to production

### Task 1: Moving the codebase to a GitHub repo

In this task, you will create a GitHub repository, generate a personal access token (PAT), initialize a local Git repository, and push the source code to GitHub using PowerShell.

1. Browse to `https://github.com` in your browser and sign in to your **personal account** using your own GitHub credentials.

   > **Note:** If you don't have a GitHub account, go to `https://github.com/join` and create one before continuing.
  
2. Create a personal access token as described below.

     - In the upper-right corner of your GitHub page, select your profile photo, then click **Settings**.

        ![](media/github-01.png)

     - From the left-hand side menu, select **Developer settings**.

        ![](media/github-devset.png)

     - Now, expand the **Personal access tokens (1)** dropdown and select **Tokens (classic) (2)**. Click the **Generate new token (3)** button on the right and select **Generate new token (classic) (4)** from the list. Provide your GitHub password if prompted.

        ![](media/pat-token1-0711.png)

     - Configure the token as follows.

        - In the **Note** field, enter **Auth-token**.

        - Under **Select scopes**, select the following:

            - `repo` - Full control of private repositories

            - `workflow` - Update GitHub Action workflows

            - `write:packages` - Upload packages to GitHub Package Registry

            - `delete:packages` - Delete packages from GitHub Package Registry

            - `read:org` - Read org and team membership, read org projects

                ![The configured scopes for a GitHub Personal Access Token.](media/github-04.png "GitHub Personal Access Token Scope Configuration")

            - Leave the other values as default and select **Generate token**.

                ![](media/github-05.png)
  
3. Click the **Copy** icon to copy the token to your clipboard and save it in Notepad. Once you leave this page, the token cannot be displayed again. **DO NOT COMMIT THIS TO YOUR REPO!**

     ![The GitHub Personal Access Token. This is a secret value that should be stored somewhere safe and accessible.](media/github-06.png "Created GitHub Personal Access Token")

4. In the upper-right corner of your GitHub page, select your profile photo, click **Repositories**, and select the **New** button at the top of the repositories list. Alternatively, you can [go straight to the new repository page](https://github.com/new).

     ![GitHub.com Landing page is shown. New button to create a new repository is highlighted.](media/E5T1S4.png "GitHub new repo")

5. Type `partsunlimited` **(1)** as your repository name. Open the visibility dropdown **(2)** and select **Private (3)** so that the repository is not publicly accessible. Select **Create repository (4)** to continue.

     ![Repository name is set to partsunlimited. Private access is selected. Create repository button is highlighted.](media/E5T1S5-0711.png "Create a new repository")

6. Select the **clipboard copy** button to copy the Git endpoint for your repository and paste the value into a text editor, such as Notepad.exe — you will need it in step 10.

     ![GitHub repository page is shown. Endpoint copy to clipboard button is highlighted.](media/updated73.png "Repository endpoint")

   >**Note:** The remaining development tasks in this exercise are performed on the lab VM.

7. Right-click the Windows Start menu and select **Windows PowerShell (Admin)** to launch a terminal window.

     ![Start Menu context menu is open. Windows PowerShell (Admin) command is highlighted.](media/launch-powershell.png "Windows PowerShell")

8. The Parts Unlimited website's source code is already copied into the VM as part of the **Before the hands-on lab exercises**. Run the command below to navigate to the source code folder.

    ```PowerShell
    cd "C:\MCW\MCW-App-modernization-microsoft-app-modernization-v2\Hands-on lab\lab-files\src"
    ```
9. Run the following command to initialize a local Git repository.

    ```PowerShell
    git init
    ```

     ![PowerShell terminal is shown. Git init is highlighted and executed.](media/updated74.png "Git init")

10. Next, register the remote endpoint as the origin of your local repository. Replace `{YourEndpointURL}` with the endpoint URL you copied from GitHub in step 6, then run the command in your PowerShell terminal.

    ```PowerShell
    git remote add origin {YourEndpointURL}
    ```

    ![PowerShell terminal is shown. Git remote add origin is highlighted and executed. ](media/updated75.png "Git remote add")

11. Run the following commands to rename the current branch to **main** and stage all the files for a commit.

    ```PowerShell
    git branch -M main
    git add .
    ```

12. Before committing, Git needs to know who you are. In the commands below, replace `John Doe` with your name and `johndoe@example.com` with your email address, then run them in your PowerShell terminal.

    ```PowerShell
    git config --global user.name "John Doe"
    git config --global user.email johndoe@example.com
    ```

13. Commit the source code to your local Git repository.

    ```PowerShell
    git commit -m "Initial Commit"
    ```

14. Push the code to GitHub by running the following command in your PowerShell terminal.

    ```PowerShell
    git push -u origin main
    ```

15. The GitHub authentication screen pops up. Switch to the **Token (1)** tab, paste the **Personal Access Token (2)** that you saved earlier, and click **Sign in (3)**.

    ![](media/pat-signin.png)

    > **NOTE**: The authentication window does not always appear — PowerShell may prompt for credentials in the terminal instead. If it does, enter your GitHub **Username**, and paste the **Personal Access Token** you copied in step 3 as the **Password**.

16. Go back to GitHub and confirm that the repository now contains the source code.

    ![GitHub is shown with the partsunlimited repository populated with source code.](media/updated77.png "GitHub repository page")

### Task 2: Creating a staging deployment slot

In this task, you will set up a staging deployment slot in your Azure App Service, which lets you test updates in a production-like environment without affecting the live site.

1. Go back to your lab resource group and search for `partsunlimited-web` **(1)** to find your Web App and App Service plan, then select the **partsunlimited-web-<inject key="DeploymentID" enableCopy="false"/> (2)** App Service resource.

   ![The search box for resources is filled in with partsunlimited-web. The partsunlimited-web-20 Azure App Service is highlighted in the list of resources in the hands-on-lab-SUFFIX resource group.](media/E5T2S1-0711.png "Resources")

2. Navigate to **Deployment slots (1)** under Deployment from the left navigation pane and select **+ Add (2)**.

    ![App Service Deployment Slots tab is open. Add slot button highlighted.](media/E5T2S2-0711.png "Deployment slots")

3. Enter **staging (1)** as the name for the new slot. Choose your App Service name from the **Clone settings from (2)** dropdown menu — this copies the production configuration to the staging environment as a starting point. Click **Add (3)** to create the new slot.

    ![Add a slot panel is open. The name is set staging. Partsunlimited-web-20 is selected for the clone settings from the dropdown list. Add button is highlighted.](media/E5T2S3-0711.png "Adding deployment slot")

4. Once you receive the success message, **Close (1)** the panel. The deployment slots list now shows the two environments **(2)** for this App Service.

    ![Successfully created slot staging message is shown. The close button is highlighted. The current list of slots is presented.](media/E5T2S4-0711.png "Deployment slots")

### Task 3: Setting up CI/CD with GitHub Actions

In this task, you will automate application deployment using GitHub Actions and the staging deployment slot you just created in Azure App Service.

1. Select your staging slot from the list of deployment slots.

2. From the toolbar menu, select **Download publish profile**. GitHub uses this publish profile to authenticate to Azure.

    ![The staging app service slot screen displays with the Get publish profile button highlighted on the toolbar.](media/E5T3S2-0711.png "Staging Publish Profile")

    >**Note:** Click **OK** on the **Download publish profile** pop-up, then repeat Step 2.

3. Open the downloaded file in a text editor and leave it open — you will copy its contents in step 6.

4. In a web browser, return to the GitHub repository for this lab, and select the **Settings** tab.

    ![The GitHub repository web page displays with Settings highlighted in the toolbar.](media/updated83.png "GitHub Repository Settings Menu")

5. From the left menu, select **Secrets and variables (1)**. Then, click on **Actions (2)** and select **New repository secret (3)**.

    ![The Secrets item is selected in the left menu and the New repository secret button is highlighted.](media/reposecret.png "New repository secret")

6. Enter the following values in the **New secret** form, then select **Add secret**.

    | Field | Value |
    |-------|-------|
    | Name  | AZURE_WEBAPP_PUBLISH_PROFILE |
    | Secret | Paste the entire contents of the publish profile you opened in the text editor in step 3. |

    ![The New secret form displays populated with the previous values. The Add secret button is highlighted.](media/E5T3S6-0711.png "Add Publish Profile Secret")

7. Select the **Actions** tab.

    ![The GitHub repository page displays with the Actions menu item highlighted.](media/updated85.png "Actions Menu")

8. On the **Get started with GitHub Actions** screen, select the **set up a workflow yourself** link.

    ![The Get started with GitHub Actions screen displays with the set up a workflow yourself link highlighted.](media/updated86.png "Set up a workflow yourself")

9. On the workflow editor screen, set the file name to **stagingdeploy.yml**. Add the content below, then commit your changes by selecting the **Commit changes** button twice.

    ```yaml
    name: Build and deploy app

    on:
      push

    env:
      AZURE_WEBAPP_NAME: partsunlimited-web-suffix   # replace suffix
      AZURE_WEBAPP_PACKAGE_PATH: '.'
      DEPLOYMENT_SLOT: staging
      DOTNET_VERSION: '3.1'
      AZURE_RESOURCEGROUP_NAME: hands-on-lab-suffix  # replace suffix

    jobs:
      build-and-deploy:
        runs-on: windows-latest
        steps:
          # Checkout the repo
          - uses: actions/checkout@main

          # Use Node.js
          - name: Use Node.js
            uses: actions/setup-node@v3
            with:
              node-version: '10.x'

          # Setup .NET Core SDK
          - name: Setup .NET Core
            uses: actions/setup-dotnet@v1
            with:
              dotnet-version: ${{ env.DOTNET_VERSION }}

          # Run dotnet build and publish
          - name: dotnet build and publish
            run: |
              dotnet build PartsUnlimited.sln --configuration Release
              dotnet publish PartsUnlimited.sln -c Release -o PartsUnlimitedWebsite

          # Deploy to Azure Web app slot
          - name: 'Run Azure webapp deploy action using publish profile credentials'
            uses: azure/webapps-deploy@v2
            with:
              app-name: ${{ env.AZURE_WEBAPP_NAME }}
              publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
              package: '${{ env.AZURE_WEBAPP_PACKAGE_PATH }}/PartsUnlimitedWebsite'
              slot-name: ${{ env.DEPLOYMENT_SLOT }}
    ```

    >**Note:** The job runs on **windows-latest**, not `ubuntu-latest`. The .NET Core 3.1 SDK needs OpenSSL 1.1, which is no longer present on the Ubuntu runner image, and the build aborts with *"No usable version of libssl was found"*.

    ![The workflow editor screen displays with the file name set to stagingdeploy.yml](media/updated87.png "Workflow naming")

10. Open the Command window and run the following commands to pull the newly created `stagingdeploy.yml` file down to your local repository, so that you can edit it in the next step.

    ```cmd
    cd "C:\MCW\MCW-App-modernization-microsoft-app-modernization-v2\Hands-on lab\lab-files\src"
    ```
    ```cmd
    git pull
    ```

    ![The image shows successfully pulling down the new GitHub workflow.](media/git-pull-workflow-file-locally.png "Pull Git Workflow")

11. Open the newly pulled file `C:\MCW\MCW-App-modernization-microsoft-app-modernization-v2\Hands-on lab\lab-files\src\.github\workflows\stagingdeploy.yml` in Visual Studio Code. Replace the `suffix` placeholder on lines 7 and 11 with **<inject key="DeploymentID" />** so that the workflow targets your lab resources. Save the file by pressing **CTRL+S**.

    ![YML file is displayed with the suffix values highlighted.](media/ymlreplacesuffix.png "YML file")

12. Commit and push the change back to the `partsunlimited` GitHub repository. Execute these commands in the WebVM Command window.

    ```cmd
    cd "C:\MCW\MCW-App-modernization-microsoft-app-modernization-v2\Hands-on lab\lab-files\src"
    git add .
    git commit -am "Updated the stagingdeploy.yml"
    git push 
    ```

13. In GitHub, select **Actions**. The workflow displays as in progress.

    ![The GitHub action workflow displays as being in progress.](media/updated88.png "Workflow in progress")

14. Once the workflow completes, go back to your lab resource group in the Azure portal and search for `staging` **(1)** to find your staging App Service slot, then select the `staging (partsunlimited-web-/staging)` **(2)** resource.

    ![The search box for resources is filled in with staging. The staging (partsunlimited-web-{uniquesuffix}/staging) Azure App Service Deployment Slot is highlighted in the list of resources in the hands-on-lab-SUFFIX resource group.](media/updated89.png "Staging Resource")

15. Notice the dedicated web link for your staging slot. Select it to browse to the site and see the result of your first deployment through the CI/CD pipeline.

    ![Staging slot for partsunlimited app service is open. URL endpoint for the deployment slot is highlighted.](media/E5T3S16-0711.png "Staging public endpoint")

### Task 4: Pushing code changes to staging and production

In this task, you will use Visual Studio Code and GitHub to push a code change to the staging environment and watch the pipeline deploy it.

1. Select the Start menu and search for **Visual Studio Code**. Select **Visual Studio Code** to run it.

    ![Start Menu is open. Visual Studio Code is typed in the search box. Visual Studio Code is highlighted from the list of search results.](media/vscode-start-menu.png "Visual Studio Code")

2. Open the **File (1)** menu and select **Open Folder... (2)**.

    ![File menu is open in Visual Studio Code. Open Folder... command is highlighted.](media/vscode-open-folder.png "Open Folder")

3. Navigate to `C:\MCW\MCW-App-modernization-microsoft-app-modernization-v2\Hands-on lab\lab-files\src` and select **Select Folder (1)**.

    ![Visual Studio Code Open Folder dialog is open. The folder path is set to C:\MCW\MCW-App-modernization-stage\Hands-on lab\lab-files\src, and the Select Folder button is highlighted.](media/selectfolder.png "Select Folder")

4. Select **Yes, I trust the authors**.

   ![.](media/clonerepo2.png)

5. Now introduce a change to the Parts Unlimited website. In the Explorer window, navigate to **src (1)** > **PartsUnlimitedWebsite (2)** > **Views (3)** > **Home**, and select **Index.cshtml (4)** for editing. Change the page title **(5)** by adding **New** at the beginning, then save the file from the **File** menu. Notice that the underlying Git repository detects the change **(6)** in the codebase.

    ![Index.cshtml from src > PartsUnlimitedWebSite > Views > Home folder is open. Page Title is changed to New Home Page. One pending change in the source control is highlighted.](media/vscode-changing-source-code.png "Code editing in VSCode")

6. Select the **Source Control (1)** tab in Visual Studio Code. The workflow file was committed directly on GitHub, so the remote repository is ahead of your local copy. Open the **Views and More Actions... (2)** menu and select **Pull (3)** to get the latest from the remote repository.

    ![Views and more actions... menu is open. Pull command is highlighted.](media/vscode-pull.png "GitHub Pull")

7. Select **Stage Changes (1)**, type a commit message **(2)** describing the change, and select **Commit (3)**.

    ![Stage changes button for index.cshtml is highlighted. Commit message is set to New Home Page Title. Commit button is highlighted.](media/vscode-stage-commit.png "GitHub Commit")

8. Open the **Views and More Actions... (1)** menu and select **Push (2)** to push the changes to GitHub.

    ![Views and more actions... menu is open. Push command is highlighted.](media/vscode-push.png "GitHub Push")

9. Open the GitHub repository and check the Actions page for the latest run of the CI/CD pipeline.

    ![PartsUnlimited repo is open. Actions page is shown. Successful CI/CD run for the new home page title is highlighted.](media/git-action.png "New commit build")

10. Once the run completes, navigate to the staging environment endpoint in your browser and observe the title change.

    ![Parts Unlimited staging environment is open in a browser. New Home Page title is highlighted.](media/updated93.png "Parts Unlimited Staging Website")

Parts Unlimited now has a separate staging environment for their e-commerce site: any new code pushed to the repository is automatically built and deployed to staging for testing.

### Task 5: Swap deployment slots to move changes in staging to production

Once Parts Unlimited is happy with the changes tested in staging, they can swap the two slots to promote those changes to production. In this task, you will perform that swap. Because a swap simply exchanges the two environments, it completes almost instantly and can be reversed by swapping back.

1. Go back to your lab resource group and search for `partsunlimited-web` **(1)** to find your App Service, then select the **partsunlimited-web-<inject key="DeploymentID" enableCopy="false"/>** **(2)** resource.

   ![The search box for resources is filled in with partsunlimited-web. The partsunlimited-web-20 Azure App Service is highlighted in the list of resources in the hands-on-lab-SUFFIX resource group.](media/5.5.1.png "Resources")

2. Switch to the **Deployment slots (1)** tab and select **Swap (2)**.

    ![App Service Deployment Slots tab is open. Swap button highlighted.](media/5.5.2.png "Deployment Slot Swap")

3. Select the **Start Swap** button to swap the staging slot with the production slot.

    ![Deployment Slot Swap dialog is open. Swap button is highlighted.](media/swap.png "Deployment Slot Swap")

4. Once you receive the success message, close the swap panel.

5. Visit both the production and staging slot endpoints and observe that the title change has moved to production.

  > **Congratulations** on completing the task! Now, it's time to validate it. Here are the steps:
	
  - Hit the Validate button for the corresponding task. If you receive a success message, you can proceed to the next task. 
  - If not, carefully read the error message and retry the step, following the instructions in the lab guide.
  - If you need any assistance, please contact us at cloudlabs-support@spektrasystems.com. We are available 24/7 to help you out.
    
<validation step="54659bb3-b86f-403f-b2fd-0cf1236e197e" />

## Summary
 
In this exercise, you have covered the following:

   - Moved the Parts Unlimited codebase to a private GitHub repository.

   - Created a staging deployment slot in Azure App Service.
   
   - Set up a CI/CD pipeline built on GitHub Actions and promoted a code change from staging to production.

Now, click on **Next** from the lower right corner to move on to the next page.

![](media/api-090-5.png "Next")
